# Production environment variables

DATA_PATH=/app/data/bestball.parquet

# JSON array of allowed CORS origins (must be valid JSON for pydantic)
ALLOWED_ORIGINS=["http://localhost:8080"]

# Leave API base URL unset so the baked frontend uses same-origin /api.
